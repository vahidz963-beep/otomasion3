# گزارش تطبیق مشتریان با سیستم فعلی

## منابع
- فایل قدیمی: 273 رکورد دارای کد
- مشتریان فعلی: 284 رکورد
- اشخاص حسابداری: 271 رکورد

## نتیجه
- تطبیق قطعی بر اساس کد قدیمی موجود در notes اشخاص حسابداری و linked_customer_id: **271**
- کدهای قدیمی موجود در finance_parties: **271**
- کدهای تکراری: **0**
- کدهای قدیمی بدون customer رکورد متناظر: **2**
- مشتریان فعلی فاقد کد قدیمی: **13**
- بازه پیشنهادی مشتریان جدید: **294 تا 306**

## نکته
کدهای قدیمی در سیستم فعلی از قبل داخل یادداشت `finance_parties` قابل مشاهده بودند؛ این گزارش هنوز هیچ UPDATE/INSERT/DELETE انجام نمی‌دهد. قبل از اجرای تغییر، باید migration کم‌خطر ساخته شود که کد را به ستون مستقل `customer_code` منتقل کند، نه اینکه رکورد مشتری یا اسناد مالی حذف شوند.

فایل‌های جزئیات: `customer_matching_final_report.csv` و `current_customers_without_legacy_code.csv`.
